import React from 'react';
import parseForm from '../utils/parseToForm.js'
const Example = () => {

  return (
    <div>
      Example
    </div>
  );
};

Example.propTypes = {
};

export default Example;
